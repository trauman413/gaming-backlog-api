// import * as igdbClient from '../../../src/services/igdbClient'

describe('sample test', () => {
  it('tests', () => {
    const result = 1 + 1
    expect(result).toBe(2)
  })
})

/**
 * Test cases:
 * --authenticate (will need to add error case) ==> 200 and not 200
 * --games Request ==> 200 and error
 */